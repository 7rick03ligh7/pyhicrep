version: 2.1

jobs:
  build_and_test:
    working_directory: ~/circleci
    docker: 
      - image: "fkrull/multi-python"
    steps:
      - checkout
      - tox

workflows:
  build-and-test:
    jobs:
      - build_and_test